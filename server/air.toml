# .air.toml

root = "./"
tmp_dir = "tmp"
build_cmd = "go build -o ./tmp/main.exe ./cmd/app/main.go"
run_cmd = "./tmp/main"

# Directories to watch
[dir]
  paths = [
    "cmd",
    "internal",
  ]

# Excluded directories/files
[exclude]
  paths = [
    "tmp",
  ]
